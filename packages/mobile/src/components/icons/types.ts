/** Props accepted by every icon component. */
export interface IconProps {
  /** Render size in logical pixels (width and height). */
  size?: number;
  /** Stroke/fill color override. */
  color?: string;
}
